pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                echo 'Hello World'
                sh "uname -a"
                sh "free -h"
            }
        }
    }
}
